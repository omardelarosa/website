{
  "name": "obs-static-site",
  "version": "1.0.0",
  "description": "Static site generator for Obsidian vault content",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "serve": "node -e \"require('http').createServer((req,res)=>{const fs=require('fs');const path=require('path');let file=req.url=='/'?'/index.html':req.url;fs.readFile('./dist'+file,(err,data)=>{if(err){res.writeHead(404);res.end('Not found');return;}const ext=path.extname(file);const types={'html':'text/html','css':'text/css','js':'application/javascript','json':'application/json'};res.writeHead(200,{'Content-Type':types[ext.slice(1)]||'text/plain'});res.end(data);})}).listen(8080,()=>console.log('Server running at http://localhost:8080'))\""
  },
  "keywords": ["obsidian", "static-site", "markdown"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "markdown-it": "^14.0.0",
    "markdown-it-wikilinks": "^1.3.2",
    "gray-matter": "^4.0.3",
    "fs-extra": "^11.2.0"
  }
}
